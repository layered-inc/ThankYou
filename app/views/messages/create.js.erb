// 先頭に足す _message.html.erb to #message
// messages/_message　をrender
// _message.html.erb内のmessageは@message
$('#messages').prepend("<%= j(render @message)%>");
$('#format-modal').modal('hide');
// メッセージの入力欄を空にする
$('#new_message')[0].reset();
